Describe the concrete problem and the resulting behavior.

Validation: list the checks actually run, their scope, and retained failures or device skips.
For numerical changes, identify the reference, same-input comparison and unchanged tolerance.
For optimizations, include measured benefit, cost and default state.
